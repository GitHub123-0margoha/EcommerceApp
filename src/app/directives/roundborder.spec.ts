import { Roundborder } from './roundborder';

describe('Roundborder', () => {
  it('should create an instance', () => {
    const directive = new Roundborder();
    expect(directive).toBeTruthy();
  });
});
